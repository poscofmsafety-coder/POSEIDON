# POSEIDON V6.2

## 1. 머리·얼굴·마스크 ROI
- 화면 표시 박스를 머리부터 턱 아래까지 확장했습니다.
- 안전모, 보안경, 마스크 판정에 필요한 영역을 한 박스에 포함합니다.
- 사람 전체 bounding box는 화면에 그리지 않지만 내부 추적/인원수/위험구역 판단용으로 유지합니다.

## 2. 작업중지권
- 사용자 모드 우측 하단에 빨간 원형 `작업중지권` 버튼을 추가했습니다.
- 지킴이 실행 중 브라우저 메모리에 최대 10초의 영상 버퍼를 유지합니다.
- 버튼을 누르면 이후 10초를 추가 기록하고, 발동자/연락처/사유와 함께 관리자에게 전송합니다.
- 영상이 브라우저/용량 제한 때문에 저장되지 않아도 사유와 현재 스냅숏은 전송됩니다.
- 사전 버퍼는 제출 전에는 서버로 업로드하지 않습니다.

## 3. 관리자 긴급 알림
- 관리자 대시보드에 작업중지권 긴급 팝업을 표시합니다.
- 이벤트 센터에 `작업중지권` 긴급 이벤트를 자동 등록합니다.
- 현장 사진 및 전후 영상 링크를 제공합니다.
- 팝업에서 바로 현장 무전을 호출할 수 있습니다.

## 4. 외부 알림(선택)
- Resend 이메일: `RESEND_API_KEY`, `ADMIN_EMAIL`, `EMAIL_FROM`
- Twilio 문자: `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER`, `ADMIN_PHONE`
- 일반 Webhook: `NOTIFY_WEBHOOK_URL`
- 외부 알림을 설정하지 않아도 관리자 대시보드/이벤트 센터 접수는 정상 동작합니다.

## 5. 데이터 정리
- 이벤트 자동 삭제 시 작업중지권 전후 영상과 작업중지 기록도 함께 정리합니다.
- 이벤트 개별/전체 삭제 및 장치 삭제에도 관련 작업중지 영상이 같이 정리됩니다.
