# POSEIDON V6.23

## Topbar readability fix
- `홈`, `현장 QR`, `AI 관제 운영 중`, `관리자`가 버튼 안에서 두 줄로 깨지지 않도록 고정했습니다.
- 상단 버튼/상태칩에 `white-space: nowrap`, `word-break: keep-all`, `flex-shrink: 0` 성격의 레이아웃을 적용했습니다.
- 날짜·시간도 데스크톱에서는 한 줄 폭을 확보해 숫자가 세로로 쪼개지지 않도록 했습니다.
- 1280px 이하의 좁은 데스크톱/태블릿 가로 화면에서는 버튼 자체를 압축하지 않고 상단 액션 그룹 전체가 별도 행으로 내려가도록 처리했습니다.
- 820px 이하 모바일은 기존 아이콘 중심 컴팩트 UI를 유지합니다.
