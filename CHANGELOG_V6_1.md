# POSEIDON V6.1

- 관제 화면 표시 박스를 작업자 전체가 아닌 머리/얼굴 ROI 중심으로 단순화했습니다. 전체 사람 박스는 내부 계산(인원수, 위험구역, 추적)에 계속 사용합니다.
- 로그인 카드를 데스크톱/모바일 모두 화면 정중앙에 배치했습니다.
- WebRTC 무전 호출에 마이크 사전 점검, 20초 호출 타임아웃, ICE 연결 상태 점검, 일시 끊김 복구 대기, 브라우저 자동재생 차단 시 `상대방 음성 재생` 버튼을 추가했습니다. TURN 설정이 존재하면 자동 사용합니다.
- 관리자 감지 규칙 카드에 항목별 `자동 음성 경보` ON/OFF를 추가했습니다.
- 관리자 대시보드 헤드라인의 `포세이돈이 위험을 포착합니다.` 문구가 데스크톱에서 한 줄로 유지되도록 가독성을 보완했습니다.
