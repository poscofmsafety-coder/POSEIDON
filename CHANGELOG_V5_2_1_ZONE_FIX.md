# POSEIDON v5.2.1 - 위험구역 OFF 수정

- 현장 장치의 `위험구역 감지` 스위치를 끄면 오버레이와 진입 감지를 즉시 중지합니다.
- 신규 장치의 위험구역 감지는 기본 OFF입니다.
- 관리자 위험구역 설정 화면에 `위험구역 사용` 스위치를 추가했습니다.
- 관리자에서 OFF로 저장하면 `rules.dangerZone=false`, `zone.enabled=false`가 함께 저장됩니다.
- PPE 오버레이는 위험구역 OFF 상태에서도 계속 작동합니다.
