# POSEIDON V6.32

## 로그인 화면 가독성
- 로그인 제목을 `POSEIDON` / `스마트 안전지킴이` 두 줄로 분리했습니다.
- 각 줄을 독립 블록으로 처리해 화면 폭이 달라져도 어색하게 중간 줄바꿈되지 않도록 했습니다.

## 상단 로그아웃 버튼
- 우측 상단 로그아웃 버튼에 `로그아웃` 텍스트를 추가했습니다.
- 기존 아이콘은 유지하고, 버튼 폭과 패딩을 확보해 날짜/관리자 버튼과 겹치지 않도록 했습니다.
- 태블릿/모바일에서도 버튼 글자가 찌그러지지 않도록 반응형 폭을 적용했습니다.
