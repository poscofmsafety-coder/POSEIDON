# POSEIDON V6.5 - PowerPoint-style Danger Zone Editor

## 위험구역 편집기
- 관리자 모드와 사용자 모드 모두 다각형/사각형 선택 지원
- 사각형 내부 드래그로 전체 이동
- 모서리 4개 + 변 중앙 4개, 총 8개 흰색 손잡이로 크기 조절
- 빨간 X 버튼으로 구역 삭제
- PC에서 Delete/Backspace 키로 구역 삭제
- 터치/마우스/펜 Pointer Events 지원
- 삭제는 실수 방지를 위해 편집 화면에서 먼저 반영되고, 저장 버튼을 눌러 서버에 적용
- 관리자 저장 시 shape 정보를 함께 저장해 사용자 화면과 동일한 형태 유지
- 관리자 프리뷰 이미지를 드래그 중 매번 재다운로드하지 않고 캐시해 이동/리사이즈를 부드럽게 처리

## UI
- 관리자 위험구역 화면에 `다각형` / `사각형 넣기` 모드 버튼 추가
- 사용자 위험구역 화면에 명시적 `선택 구역 삭제` 버튼 추가
- 모바일/태블릿에서도 편집 컨트롤이 한 열로 재배치
